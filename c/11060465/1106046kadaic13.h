#include <ctime>

#include <string>
using namespace std;

// 関数のプロトタイプ宣言
int getDigit(bool right);
string getAnswer();
bool isNumber(string s);
bool judgeAnswer(int ans, int right);
void showResult(bool result);